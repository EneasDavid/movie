# Go
/bin/
*.test

# Node / Vite (web/node_modules is reinstalled from package-lock.json;
# internal/web/static IS committed on purpose — see README — it's the
# go:embed source and must exist for `go build` to work on a fresh clone
# even before anyone runs the frontend build)
web/node_modules/
web/dist/

# Env
.env
.env.local
.env.*.local

# Google service account keys (cmd/fixaudio) — a credential file, never
# committed, same as .env.
*service-account*.json
movie-privet-*.json

# OS
.DS_Store
